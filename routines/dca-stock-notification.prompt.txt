DCA WEEKLY STOCK DASHBOARD — OPERATING INSTRUCTIONS

You are running as an unattended scheduled task for Vincent William (PT. Duta Cendana
Adimandiri, Suzuki dealer). Fresh session, no memory of prior runs. Ask no questions;
make reasonable decisions and proceed. Everything you need is in this checkout.

--------------------------------------------------------------------------------
THE ONE RULE THAT MATTERS MOST
--------------------------------------------------------------------------------
This pipeline moves TEXT ONLY. It must never produce, encode, attach or emit a
binary payload.

  - Do NOT render a PDF.
  - Do NOT call mcp__Gmail__send_message with an `attachments` array.
  - Do NOT call mcp__Google_Drive__create_file with `base64Content`.
  - Do NOT emit any long verbatim high-entropy string as a tool-call argument.

An earlier design attached the stock report as a PDF. The stock table runs to ~176
rows across 21 columns; the resulting PDF base64 is far past the length at which a
single verbatim emission stops completing, so the run hung with the email unsent —
every time, not intermittently. Read `.claude/skills/base64-binary-payloads/SKILL.md`
and the "Why there is no PDF" section of README.md before changing anything about
delivery. The dashboard travels as the email's own HTML body, which is an ordinary
UTF-8 string parameter with no encoding step and no size cliff.

If you ever find yourself reaching for base64 here, the design has been broken.
Stop and report instead.

--------------------------------------------------------------------------------
STEP 0 — Preflight
--------------------------------------------------------------------------------
Run `ls` and locate this repository's checkout (it contains src/, routines/, tests/).
Work from its root. Confirm `python3 --version` responds. No pip install is needed —
the pipeline is pure standard library, deliberately.

Run the test suite first:  python3 tests/test_parse_stock.py
It must print "PASS". If it fails, STOP: send nothing, and report the failure. A
parser that cannot reproduce its known-good fixture must not be pointed at live data.

--------------------------------------------------------------------------------
STEP 1 — Fetch the source email
--------------------------------------------------------------------------------
You run Friday evening. The upstream system ("ArUnit") emails a stock notification
every morning at about 09:00 WIB / 02:00 UTC, so the newest one is normally this same
Friday morning's. Find it:

  mcp__Gmail__search_threads with
    query: subject:"Notifikasi Stock" newer_than:5d
    pageSize: 10

Take the NEWEST result. Its own date is the report's "as of" date.

Senders seen historically: stockdcasystem@gmail.com and ar.dutacendana@gmail.com.
Accept either; do not filter on sender, filter on the subject and take the newest.

Then read it in full:

  mcp__Gmail__get_thread with messageFormat: PLAIN_TEXT

PLAIN_TEXT matters: it renders the HTML table as a markdown pipe table, which is
exactly what the parser expects. Do not request FULL_CONTENT — the HTML body is
~480 KB and will flood your context for no benefit.

--------------------------------------------------------------------------------
STEP 2 — Guard: is the data fresh enough?
--------------------------------------------------------------------------------
The upstream system occasionally skips a day. At a weekly cadence, skipping the whole
week because Friday's send was missed is worse than reporting Thursday's position — so
accept the newest notification from the last 4 days and label it honestly.

  - Newest notification is 0-4 days old  -> proceed. The report is titled "per
    <that email's date>", never "per today". If it is not from today, say so in your
    STEP 8 summary.
  - Newest notification is older than 4 days, or there is none -> SILENT NO-OP.
    Send no email, publish nothing, report "no recent source email" and end.

Never substitute today's date for the source email's date, and never carry numbers
forward from a previous week.

--------------------------------------------------------------------------------
STEP 3 — Parse
--------------------------------------------------------------------------------
Write the `plaintextBody` you received to a working file, then:

  mkdir -p out
  python3 src/parse_stock.py out/source.md YYYY-MM-DD > out/metrics.json

where YYYY-MM-DD is the source email's date. The parser raises on a missing or empty
table rather than inventing numbers — if it raises, STOP and report.

--------------------------------------------------------------------------------
STEP 4 — Sanity gates (do not skip)
--------------------------------------------------------------------------------
Read out/metrics.json and check all four before going further:

  1. rows_in_table is at least 50. A sudden collapse to a handful of rows means the
     upstream format changed, not that the dealer sold its inventory overnight.
  2. Every entry in `reconciliation` has "match": true. These compare the source
     email's own four headline counters against a recount of its own table. A
     mismatch means either the upstream summary is wrong or the parse is wrong —
     either way a human should look before it goes out.
  3. counts.unsold is greater than 0.
  4. value.unsold_hpp is greater than 0.

If any gate fails: publish nothing, send nothing to the distribution list. Email
ONLY vwilliam@dutacendana.com with the subject prefixed "[PERIKSA]", stating exactly
which gate failed and the figures involved. Then end.

--------------------------------------------------------------------------------
STEP 5 — Render
--------------------------------------------------------------------------------
  python3 src/render_dashboard.py out/metrics.json

This writes three files, none of them binary:

  out/artifact.html   the dashboard, with HPP -- published, not emailed
  out/email-a.html    email A body, WITH HPP cost figures
  out/email-b.html    email B body, NO cost figures anywhere

Do not hand-edit any of them; change the renderer instead so the next run inherits
the fix. In particular, never produce variant B by editing variant A -- the renderer
removes whole columns and swaps the value tile for the sold count, which blanking
cells by hand would not reproduce.

The command prints a SENTINEL for each body, e.g.

  <!-- dca-stok v1 a 2026-09-11 51c5126a98547957 -->

It is the last line of the file and carries a hash of everything above it. Write both
sentinels down now -- STEP 7 will not let you send without them.

--------------------------------------------------------------------------------
STEP 6 — Send the email
--------------------------------------------------------------------------------
Duplicate guard first -- this is now the first outward-facing step in the run.
Search sent mail:

  mcp__Gmail__search_threads with query: in:sent subject:"Stok Mingguan DCA" newer_than:9d

Then filter what comes back, in this order. Both filters are mandatory.

FIRST, BY REPORT DATE. Every subject ends "per <tanggal>". Read that date out of each
result and DISCARD every message whose date is not this run's report date. Only the
survivors count as already-sent.

This is not pedantry. The window is deliberately wider than a week, so last week's
report is normally in the results, and a run that treats any recent "Stok Mingguan DCA"
as its own would skip the week in silence. It nearly happened on 18 September 2026: the
11 September report was not actually delivered until the 14th, four days before the next
run, so it sits right inside a narrower window too. A skipped week produces no error and
no email -- nobody finds out until someone notices the report never came.

SECOND, BY RECIPIENT. Among the survivors, the check is PER RECIPIENT, not per subject.
A and B share a subject, so a subject-only check would let a run that sent A and failed
on B mark the week complete and silently never deliver B. Look at who each surviving
message actually went to, and send only the missing side.

If both lists are covered by surviving messages, end and report "already delivered for
<report date>". If nothing survives the first filter, this week has not been sent at
all -- proceed normally, however many older reports the search returned.

NEVER SEND A PLACEHOLDER. On 4 September 2026 a run sent list A a body that read
`<html><body> PLACEHOLDER </body></html>`, then a correction two minutes later titled
"koreksi isi email sebelumnya". Five people, including the owner and finance, got a
broken email from an automation they are supposed to trust.

So: the ONLY thing that may ever go in htmlBody is the exact, complete contents of the
rendered file. Not a stub, not a shortened version, not a "test first then correct",
not a hand-retyped summary. If for any reason you cannot paste the whole file, SEND
NOTHING and report it -- a missing report is recoverable, a wrong one already in five
inboxes is not.

PRE-SEND GATE, both emails, no exceptions: the last line of the text you are about to
pass as htmlBody must be character-for-character the sentinel STEP 5 printed for that
variant. If the last line is anything else, you are not holding the report: stop, do
not send, re-read the file and try again. Check this immediately before each send,
against the body as you have actually assembled it -- not against the file on disk.

TWO SENDS GO OUT, sharing one subject. They differ only in whether cost figures
are present. Send both with mcp__Gmail__send_message:

  --- Email A: with HPP ---
  to:        vwilliam@dutacendana.com, stock@suzukidutacendana.com,
             fineke99@gmail.com, om@suzukidutacendana.com,
             finance@suzukidutacendana.com
  subject:   Stok Mingguan DCA — per <tanggal, e.g. 29 Agustus 2026>
  htmlBody:  the full contents of out/email-a.html
  body:      short plain-text fallback; may include the stock value
  attachments: OMIT THIS FIELD ENTIRELY.

  --- Email B: without HPP ---
  to:        it@dutacendana.com, all.bm@suzukidutacendana.com,
             m.rizky@smkwikrama.sch.id
  subject:   the SAME subject as A
  htmlBody:  the full contents of out/email-b.html
  body:      short plain-text fallback with NO rupiah figure in it
  attachments: OMIT THIS FIELD ENTIRELY.

Before sending B, grep its body for "Rp" and for "HPP" and confirm both return
nothing. Variant B exists so that cost data does not reach that audience; sending A
to B's recipients is the one failure here that cannot be taken back. If the check
finds anything, send A only and report the problem.

Never put a recipient on both emails, and never CC across the two lists.

`all.bm@suzukidutacendana.com` is a group alias that expands to the branch managers.
Treat it as list B and nothing else. If the alias is ever changed to include someone
who is also on list A, that person receives both emails — harmless, since A is the
fuller report. The failure that matters runs the other way: never add a group alias to
list A without knowing every address inside it, because one B-audience member hidden
in an A alias leaks the cost figures to them.

The htmlBody is passed as an ordinary string. It is roughly 30 KB of readable markup —
low entropy, no encoding, and it has no reliability cliff. This is the whole point of
the design.

If the send fails, retry once. If it fails again, report the failure plainly. Do not
fall back to attaching anything.

--------------------------------------------------------------------------------
STEP 6.5 — Verify what actually landed (do not skip)
--------------------------------------------------------------------------------
A send that returns an id only proves Gmail accepted something. Prove it accepted the
report. After BOTH sends, re-read the sent copies:

  mcp__Gmail__search_threads with query: in:sent subject:"Stok Mingguan DCA" newer_than:1d

Apply the same report-date filter as STEP 6 -- you are verifying the two messages YOU
just sent, not whatever else is in the window. For each of those two messages, check the metadata the search already returns -- this
costs nothing and needs no second fetch:

  1. sizeEstimate is above 15000. The real bodies run 24-31 KB. The placeholder that
     went out on 4 September was 1,363 bytes, so this single check would have caught
     it outright.
  2. snippet begins with "PT. Duta Cendana Adimandiri". A stub, an empty body or a
     truncated paste all fail this.
  3. toRecipients matches the list that message was meant for, exactly.

If any message fails 1 or 2, its body is not the report. Send that variant again
immediately, with the correct body and the subject suffixed " (koreksi isi email
sebelumnya)", then say so plainly in STEP 8 -- never leave a broken report standing as
the week's delivery.

If a message fails 3, do not resend blindly: say which addresses got what, because a
list-A body reaching a list-B address is a cost-data leak and needs a person, not a
retry.

--------------------------------------------------------------------------------
STEP 7 — Do NOT publish the Artifact on a scheduled run
--------------------------------------------------------------------------------
Skip this step. Do not call the Artifact tool. The run is finished after STEP 6.5.

This is not an oversight. The Artifact tool raises a permission prompt that a
scheduled run cannot answer: the prompt is raised by the harness before the tool
runs, so no instruction in this file -- however emphatic -- suppresses it, and there
is no setting anywhere in the routine, the CLI or the API that pre-authorises it. It
blocked the 4 September run for three hours and the 11 September run for nearly three
days, confirmed by the account owner, who saw the prompt waiting.

Nothing is lost by skipping it. The Artifact is the owner's reference copy, its link
is never sent to anyone, and both email bodies are complete on their own. The owner
refreshes it on demand by asking an interactive session to run the pipeline and
publish out/artifact.html to the URL in routines/dca-stock-notification.md -- a
session with a human in it can answer the prompt in one click.

If you are NOT a scheduled run -- a person is present and has explicitly asked for a
refreshed dashboard -- then publishing is fine: read the existing artifact first
(action: "read" with the URL from routines/dca-stock-notification.md), publish
out/artifact.html to that same URL so the link stays stable, omit the favicon, and
label it with the report date.

--------------------------------------------------------------------------------
STEP 8 — Report
--------------------------------------------------------------------------------
Summarise in a few lines: report date, rows parsed, unsold count, HPP tied up, count
over 90 days, any data-quality findings, and the recipients of each
of the two emails, stated separately so it is clear both went out. State the
STEP 6.5 result for each message -- its sizeEstimate and whether the snippet check
passed. "Sent" is not the same claim as "verified delivered intact", and only the
second one is worth reporting.
Do not commit anything except an artifact URL first written in STEP 6.

--------------------------------------------------------------------------------
WHAT THE DASHBOARD SAYS, AND WHY
--------------------------------------------------------------------------------
The source email already lists every unit. Restating that list adds nothing. What it
does NOT do, and what this report exists to add:

  - Aging. Days since DO, bucketed, for unsold units only. Capital sitting in metal
    older than 90 days is the single number a dealer principal acts on, and a weekly
    rhythm is the right one for acting on it.
  - Value. HPP rolled up per bucket, per branch, per model — so "8 old units" becomes
    "Rp 1,48 M tied up".
  - Reconciliation. The source's "Total Stock" counter is FREE + MATCHING, not the
    row count. Rows that are SOLD or carry no status at all are in the same table and
    are counted by no headline figure. The dashboard states this explicitly rather
    than letting the reader assume.
  - Data quality. Duplicate DO numbers, blank chassis/engine/location, placeholder
    rows priced at Rp 0 or Rp 123. These are upstream defects worth someone's
    attention, and they are invisible in a 176-row wall of text.
